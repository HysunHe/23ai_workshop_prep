问题： Exadata,优化,什么情况下会使用存储索引？
解答： 为了使用存储索引，查询必须满足以下条件：
1. 存储索引只能与执行智能扫描的语句一起使用。主要要求是优化器必须选择全扫描，并且 I/O 必须通过直接路径读取机制完成。 
2. 为了使语句使用存储索引，必须有包含至少一个谓词的 WHERE 子句。
3. 存储索引可与以下运算符一起使用：=、<、>、BETWEEN、>=、<=、IN、IS NULL、IS NOT NULL