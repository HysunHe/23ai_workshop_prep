问题： Oracle12c-21C ,开发 （develop feature),在ADG环境私有临时是否在备库端正确存取
解答： 在ADG 环境是比较适合使用私有临时表的。ADG 环境的备库一般都是只读模式下打开，作为查询或备份的站点，在查询过程中有一些计算结果或者缓存数据就可以使用私有临时表临时缓存。私有临时表，一个在内存中用完即弃的临时对象。如果开发人员需要为不同的事务创建不同的临时表，或在只读数据库使用临时表时，都可以使用私有临时表。这里创建的私有临时表跟主库是完全没有关系的，它是查询业务中一个数据缓存空间。